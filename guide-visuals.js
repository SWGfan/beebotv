(()=>{const triggers=[...document.querySelectorAll('.screen-zoom')];if(!triggers.length)return;const modal=document.createElement('dialog');modal.className='guide-lightbox';modal.setAttribute('aria-label','Enlarged guide screenshot');document.body.append(modal);triggers.forEach(button=>button.addEventListener('click',()=>{modal.replaceChildren();const close=document.createElement('button');close.type='button';close.className='lightbox-close';close.textContent='Close ×';close.addEventListener('click',()=>modal.close());const title=document.createElement('h2');title.textContent=button.closest('.visual-guide').querySelector('h3').textContent;modal.append(close,title,button.querySelector('.screen-stage').cloneNode(true));modal.showModal();close.focus()}));modal.addEventListener('click',e=>{if(e.target===modal){const r=modal.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)modal.close()}})})();
